import { useState } from 'react'

export const use<%= hookName %> = () => {
  const [visible, setVisble] = useState<boolean>(false)

  const handleOnClick = () => {
    setVisble(!visible)
  }

  return {
    visible,
    handleOnClick
  }
}
